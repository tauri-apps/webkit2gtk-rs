name: github pages

on:
  push: 
    branches: [master]
      
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install libwebkit2gtk-4.0-dev -y
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: doc
<<<<<<< HEAD
          args: --package webkit2gtk --package webkit2gtk-sys --features=v2_28 --no-deps
=======
          args: --package webkit2gtk --package webkit2gtk-sys --no-deps
>>>>>>> a5a97f7... make_regen & remove unused UserContentFilter

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v2
        env:
          PERSONAL_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PUBLISH_BRANCH: gh-pages
          PUBLISH_DIR: ./target/doc/
